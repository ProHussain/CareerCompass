<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CareerCompass" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary Colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Text Colors -->
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textColorSecondary">@color/colorTextSecondary</item>
        <item name="android:fontFamily">@font/lato_regular</item>

        <!-- Background Colors -->
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:colorBackground">@color/colorBackgroundSecondary</item>

        <!-- Button Colors -->
        <item name="colorButtonNormal">@color/colorButtonPrimary</item>

        <!-- Divider Color -->
        <item name="android:divider">@color/dividerColor</item>
        <item name="android:dividerHorizontal">@color/dividerColor</item>
        <item name="android:dividerVertical">@color/dividerColor</item>


    </style>

    <style name="Theme.CareerCompass" parent="Base.Theme.CareerCompass" />
</resources>